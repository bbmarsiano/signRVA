// attach-p7s — bundle CMS detached .p7s signature with signed PDF for archival
export async function attachP7s(
  _pdfBytes: Uint8Array,
  _p7sBytes: Uint8Array
): Promise<Uint8Array> {
  return new Uint8Array();
}
